# ConfigTool Frontend Environment Variables
# Copy this file to .env.local for local overrides (not committed to git)

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Backend API URL
# - Development: http://localhost:8080
# - Staging: https://configtool-backend-staging.onrender.com
# - Production: https://configtool-backend.onrender.com
VITE_API_URL=

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# WebSocket URL for real-time updates
# Defaults to the API URL with ws:// or wss:// protocol if not set
# - Development: ws://localhost:8080
# - Staging: wss://configtool-backend-staging.onrender.com
# - Production: wss://configtool-backend.onrender.com
VITE_WS_URL=

# Environment identifier (development, staging, production)
# Used for conditional logic and debugging
VITE_ENV=

# =============================================================================
# USAGE
# =============================================================================
#
# Vite automatically loads environment files based on mode:
# - .env                 - Loaded in all cases
# - .env.local           - Loaded in all cases, ignored by git
# - .env.[mode]          - Only loaded in specified mode
# - .env.[mode].local    - Only loaded in specified mode, ignored by git
#
# Available modes and scripts:
# - npm run dev          - Uses .env.development (mode: development)
# - npm run build        - Uses .env.production (mode: production)
# - npm run build:staging - Uses .env.staging (mode: staging)
# - npm run preview      - Uses .env.production (mode: production)
#
# To override environment variables locally without committing:
# 1. Copy this file to .env.local
# 2. Set your custom values
# 3. .env.local takes priority over .env.[mode]
